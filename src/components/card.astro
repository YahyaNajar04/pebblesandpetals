---
const { shadow = "var(--color-sunny)", class: extra = ""} = Astro.props;
---
<div
      class = {`rounded-2xl border-3 border-ink bg-white p-6 text-center ${extra}`}
      style = {`box-shadow: 6px 6px 0 ${shadow}`}
      >
      <slot />
</div>